# Shared Contracts

These TypeScript files define the first typed boundary for the app.

Current scope:

- frontend-to-backend API request and response shapes
- shared public runtime configuration

Current rule:

- introduce types at boundaries first
- keep runtime JavaScript stable until the contracts stop changing
